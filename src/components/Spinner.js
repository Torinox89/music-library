import spinner_image from '../spinner_image.gif'

const Spinner = () => {
    return (
        <div>
            <img src={spinner_image} alt="loading..." />
        </div>
    )
}

export default Spinner